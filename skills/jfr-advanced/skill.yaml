schemaVersion: 1

name: jfr-advanced
version: 1.3.0
description: >-
  Engineering JDK Flight Recorder evidence beyond stock settings: discovering event schemas
  and settings on the target build, designing threshold/period/throttle/stack trade-offs,
  composing and validating JFC configurations, accounting for concurrent recordings, defining
  low-cost custom events and relational metadata, operating Recording/RecordingStream/MXBean
  consumers, and validating loss, parsing, retention, privacy, and Java 25 JFR features. Use
  when an event is absent, a field/parser is guessed, fine events are thresholded away, custom
  instrumentation or live export is proposed, or recording overhead/coverage is uncertain.
  Does not own first-tool selection, async-profiler, or fleet continuous-profiling operations.

license: Apache-2.0

authors:
  - name: agent-skills contributors
    url: https://github.com/robsonkades/agent-skills

homepage: https://github.com/robsonkades/agent-skills/tree/main/skills/jfr-advanced
repository:
  type: git
  url: https://github.com/robsonkades/agent-skills
  directory: skills/jfr-advanced

keywords:
  - jfr
  - flight-recorder
  - jfc
  - custom-events
  - recordingstream
  - profiling
  - jvm

files:
  - SKILL.md
  - skill.yaml
  - references/

dependencies:
  - name: jfr-and-async-profiler
    version: ^1.0.0

suggests:
  - virtual-threads-internals
  - continuous-profiling
  - async-profiler-advanced

capabilities:
  - reads-source

agentOverrides:
  codex:
    interface:
      display_name: JFR Advanced
      short_description: Custom jfc files, custom events and streaming
      default_prompt: Use JFR Advanced to configure a recording that can prove what this investigation needs.
