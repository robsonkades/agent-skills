schemaVersion: 1

name: lock-inflation
version: 1.2.0
description: >-
  Diagnosing and engineering Java intrinsic-monitor contention across fast and inflated monitor
  states without freezing one HotSpot release's internals. Covers ownership, recursion, wait sets,
  entry queues, inflation/deflation, virtual-thread behavior, JFR/thread-dump evidence,
  threshold/censoring, convoys, fairness, lock graphs, critical-section redesign, partitioning and
  validation. Use when `synchronized` wait/hold time is suspected; JMM correctness, explicit locks,
  false sharing and lock-free algorithms have separate owners.

license: Apache-2.0

authors:
  - name: agent-skills contributors
    url: https://github.com/robsonkades/agent-skills

homepage: https://github.com/robsonkades/agent-skills/tree/main/skills/lock-inflation
repository:
  type: git
  url: https://github.com/robsonkades/agent-skills
  directory: skills/lock-inflation

keywords:
  - java
  - concurrency
  - synchronized
  - objectmonitor
  - contention
  - jfr
  - locking

files:
  - SKILL.md
  - skill.yaml
  - references/

dependencies:
  - name: java-memory-model
    version: ^1.0.0

suggests:
  - java-thread-safety-contracts
  - concurrency-diagnostics
  - lock-free-patterns
  - false-sharing-and-contended
  - jvm-performance-review

capabilities:
  - reads-source

agentOverrides:
  codex:
    interface:
      display_name: Lock Inflation
      short_description: Monitor contention, inflation and lock scope
      default_prompt: Use Lock Inflation to decide whether this synchronized section is costing latency.
