schemaVersion: 1

name: cancellation-and-interruption
version: 1.1.0
description: >-
  Designing cooperative cancellation in Java across interruption, Future/CompletableFuture,
  executor/scope shutdown, deadlines, resource close/abort, CPU loops, blocking APIs, native calls,
  partial side effects and cleanup. Covers multiple cancellation sources, signal ownership,
  propagation/translation/restoration, noninterruptible regions, residual work, idempotency and
  bounded termination tests. Use when timeout/cancel returns but work or resources remain, or when
  `InterruptedException` handling is ambiguous. Timeout selection and retry policy are separate.

license: Apache-2.0

authors:
  - name: agent-skills contributors
    url: https://github.com/robsonkades/agent-skills

homepage: https://github.com/robsonkades/agent-skills/tree/main/skills/cancellation-and-interruption
repository:
  type: git
  url: https://github.com/robsonkades/agent-skills
  directory: skills/cancellation-and-interruption

keywords:
  - java
  - concurrency
  - interrupt
  - cancellation
  - interruptedexception
  - future
  - timeout

files:
  - SKILL.md
  - skill.yaml
  - references/

dependencies:
  - name: timeouts-and-deadlines
    version: ^1.0.0

capabilities:
  - reads-source

agentOverrides:
  codex:
    interface:
      display_name: Cancellation and Interruption
      short_description: Interrupt protocol, cancel semantics, uncancellable work
      default_prompt: Use Cancellation and Interruption to check whether this work can actually be stopped.
