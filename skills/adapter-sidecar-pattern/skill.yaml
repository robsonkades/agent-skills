schemaVersion: 1

name: adapter-sidecar-pattern
version: 2.3.0
description: >-
  Choose and review Kubernetes telemetry adapters when a legacy or vendor process emits
  incompatible metrics, logs or health signals, when deciding between per-pod translation
  and a node agent, or when an application upgrade silently changes parsed telemetry.
  Covers translation contracts, evidence-backed enrichment and failure behavior. Excludes
  in-process interface adaptation (gof-adapter), container mechanics (sidecar-pattern),
  probe configuration (kubernetes-service-lifecycle) and telemetry instrumentation design.

license: Apache-2.0

authors:
  - name: agent-skills contributors
    url: https://github.com/robsonkades/agent-skills

homepage: https://github.com/robsonkades/agent-skills/tree/main/skills/adapter-sidecar-pattern
repository:
  type: git
  url: https://github.com/robsonkades/agent-skills
  directory: skills/adapter-sidecar-pattern

keywords:
  - kubernetes
  - sidecar
  - observability
  - logs
  - metrics
  - daemonset

files:
  - SKILL.md
  - skill.yaml
  - references/

suggests:
  - sidecar-pattern
  - gof-adapter
  - distributed-tracing-design
  - kubernetes-service-lifecycle
  - metrics-and-cardinality
  - structured-logging

capabilities:
  - reads-source

agentOverrides:
  codex:
    interface:
      display_name: Adapter Sidecar Pattern
      short_description: Normalising what a container emits
      default_prompt: Use Adapter Sidecar Pattern to review how this workload's telemetry reaches the platform.
