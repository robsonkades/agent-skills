schemaVersion: 1

name: concurrency-limiting-and-bulkheads
version: 1.3.1
description: >-
  Engineer process-local concurrency limits and bulkheads around scarce resources, with explicit
  admission deadlines, permit ownership, weighted work, partitioning, fairness, observability and
  overload validation. Distinguishes concurrency, rate and queue limits and the assumptions behind
  Little's Law. Use after virtual-thread migrations, during downstream saturation, or when local
  limits leak, over-release, double-queue or fail to compose across replicas.

license: Apache-2.0

authors:
  - name: agent-skills contributors
    url: https://github.com/robsonkades/agent-skills

homepage: https://github.com/robsonkades/agent-skills/tree/main/skills/concurrency-limiting-and-bulkheads
repository:
  type: git
  url: https://github.com/robsonkades/agent-skills
  directory: skills/concurrency-limiting-and-bulkheads

keywords:
  - java
  - concurrency
  - semaphore
  - bulkhead
  - rate-limiting
  - backpressure
  - resilience

files:
  - SKILL.md
  - skill.yaml
  - references/

dependencies:
  - name: littles-law-and-queueing
    version: ^1.0.0
  - name: thread-sizing-and-virtual-threads
    version: ^1.0.0

suggests:
  - retries-and-backoff
  - rate-limiting-and-load-shedding
  - distributed-locks-and-leases
  - failure-models

capabilities:
  - reads-source

agentOverrides:
  codex:
    interface:
      display_name: Concurrency Limits & Bulkheads
      short_description: Semaphores, bulkheads and where the limit belongs
      default_prompt: Use Concurrency Limiting and Bulkheads to bound this in-flight work next to the resource it protects.
