schemaVersion: 1

name: gof-facade
version: 1.2.3
description: >-
  Facade in modern Java: one coherent entry point over a subsystem of collaborators, so callers
  depend on an intention rather than on a sequence. Covers the difference between a facade
  (simplifies, does not forbid) and a boundary (forbids), the god-facade drift where one class
  accumulates unrelated use cases, how application services and gateways can play this role
  while retaining their own boundary responsibilities, and the transaction and fan-out
  decisions a facade method silently owns. Use when callers repeat the same orchestration sequence,
  when a legacy subsystem needs fencing, when unrelated responsibilities accumulate in a service,
  or when a facade method fans out to remote services. Does not cover changing one type's
  interface (gof-adapter), adding behaviour to one object (gof-decorator), hub-based coordination
  between peers (gof-mediator), the coarse-grained remote boundary and its DTOs
  (remote-facade-and-dto), or transaction-boundary mechanics (enterprise-transactions).

license: Apache-2.0

authors:
  - name: agent-skills contributors
    url: https://github.com/robsonkades/agent-skills

homepage: https://github.com/robsonkades/agent-skills/tree/main/skills/gof-facade
repository:
  type: git
  url: https://github.com/robsonkades/agent-skills
  directory: skills/gof-facade

keywords:
  - design-patterns
  - gof
  - structural
  - facade
  - application-service
  - boundaries

files:
  - SKILL.md
  - skill.yaml
  - references/

dependencies:
  - name: gof-adapter
    version: ^1.0.0
  - name: gof-decorator
    version: ^1.0.0

suggests:
  - architecture-testing
  - connection-pool-sizing
  - domain-logic-organization
  - enterprise-transactions
  - event-driven-architecture
  - failure-models
  - gof-mediator
  - idempotency
  - java-composition-over-inheritance
  - java-resource-management
  - java-testing-strategy
  - query-objects-and-specifications
  - remote-facade-and-dto
  - scatter-gather
  - service-layer-design
  - structured-concurrency

capabilities:
  - reads-source

agentOverrides:
  codex:
    interface:
      display_name: Facade
      short_description: One entry point, before it becomes the app
      default_prompt: Use Facade to review this coordinating class.
