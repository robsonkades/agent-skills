schemaVersion: 1

name: queueing-models
version: 1.3.0
description: >-
  Choosing, parameterising and falsifying queueing models: M/M/1, M/M/c, M/G/1, finite/loss
  and closed networks; Erlang C/B, Pollaczek–Khinchine, Kingman/Allen–Cunneen, variability,
  queue topology and what model assumptions permit.
  Use when a predicted wait time disagrees with the measured one, when latency is far worse
  than utilisation suggests, when service times are bimodal or GC-spiked, when arrivals are
  retries or cron bursts rather than independent users, when Erlang C must be computed for a
  large number of servers, when routing or partitioning changes the queue topology, or when
  deciding whether a measured tail can be inferred from an analytical model. Does not cover
  the `L = λW` conservation law or operational pool sizing
  (littles-law-and-queueing), the alpha/beta scalability model
  (universal-scalability-law), or the statistics of the measured numbers
  themselves (latency-statistics).

license: Apache-2.0

authors:
  - name: agent-skills contributors
    url: https://github.com/robsonkades/agent-skills

homepage: https://github.com/robsonkades/agent-skills/tree/main/skills/queueing-models
repository:
  type: git
  url: https://github.com/robsonkades/agent-skills
  directory: skills/queueing-models

keywords:
  - performance
  - queueing
  - erlang
  - kingman
  - latency
  - utilisation
  - modelling

files:
  - SKILL.md
  - skill.yaml
  - references/

dependencies:
  - name: littles-law-and-queueing
    version: ^1.0.0
  - name: universal-scalability-law
    version: ^1.0.0

suggests:
  - capacity-planning
  - coordinated-omission
  - kafka-consumers-in-java
  - kubernetes-service-lifecycle
  - load-testing
  - retries-and-backoff
  - universal-scalability-law

capabilities:
  - reads-source

agentOverrides:
  codex:
    interface:
      display_name: Queueing Models
      short_description: Picking and reading M/M/c, M/D/1 and Kingman
      default_prompt: Use Queueing Models to choose the queueing model this system actually satisfies and read it correctly.
