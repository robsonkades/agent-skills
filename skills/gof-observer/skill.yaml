schemaVersion: 1

name: gof-observer
version: 1.2.0
description: >-
  Observer in modern Java, treated as high-risk: notifying an unknown set of dependents that
  something changed, and the guarantees it does not provide. Covers the properties people assume
  and the contract does not give — synchrony on the publisher thread, unspecified ordering, one
  exception breaking the rest, no delivery guarantee — the listener leak from a long-lived
  subject, reentrant registration during notification, never notifying while holding a lock, and
  the differences between an in-process observer, a reactive stream, and distributed pub/sub. Use
  when a listener mechanism is added, when a listener leak appears, when an in-process listener is
  moved to a message broker, when order between listeners matters, or when a failing listener
  silently loses work. Does not cover Spring's event phases and the outbox
  (event-driven-architecture), broker delivery semantics (delivery-semantics), reactive
  backpressure (reactive-backpressure), or hub-based coordination (gof-mediator).

license: Apache-2.0

authors:
  - name: agent-skills contributors
    url: https://github.com/robsonkades/agent-skills

homepage: https://github.com/robsonkades/agent-skills/tree/main/skills/gof-observer
repository:
  type: git
  url: https://github.com/robsonkades/agent-skills
  directory: skills/gof-observer

keywords:
  - design-patterns
  - gof
  - behavioral
  - observer
  - events
  - listeners

files:
  - SKILL.md
  - skill.yaml
  - references/

dependencies: []

suggests:
  - allocation-profiling
  - delivery-semantics
  - distributed-locks-and-leases
  - distributed-tracing-design
  - distributed-transactions-and-sagas
  - event-driven-architecture
  - gof-command
  - heap-dump-analysis
  - java-memory-model
  - message-ordering-and-partitioning
  - poison-messages-and-dlq
  - rpc-and-api-contracts
  - scoped-values
  - slo-and-alerting
  - structured-logging

capabilities:
  - reads-source

agentOverrides:
  codex:
    interface:
      display_name: Observer
      short_description: Notification without the guarantees you assumed
      default_prompt: Use Observer to review this notification mechanism.
