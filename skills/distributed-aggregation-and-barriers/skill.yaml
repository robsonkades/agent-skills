schemaVersion: 1

name: distributed-aggregation-and-barriers
version: 1.3.0
description: >-
  Correct and recoverable aggregation across workers: algebraic laws, duplicate attempts,
  numeric reproducibility, mergeable summaries, barriers, joins, skew, checkpointing and
  partial results. Use when totals drift between runs, stragglers set job latency, worker
  percentiles are averaged, cardinality exhausts memory, or a join stalls on one task. It
  excludes request fan-out, streaming windows, percentile theory, message ordering and the
  broader hot-key repair catalogue.

license: Apache-2.0

authors:
  - name: agent-skills contributors
    url: https://github.com/robsonkades/agent-skills

homepage: https://github.com/robsonkades/agent-skills/tree/main/skills/distributed-aggregation-and-barriers
repository:
  type: git
  url: https://github.com/robsonkades/agent-skills
  directory: skills/distributed-aggregation-and-barriers

keywords:
  - aggregation
  - barrier
  - reduce
  - straggler
  - hyperloglog
  - distributed-join
  - java

files:
  - SKILL.md
  - skill.yaml
  - references/

dependencies:
  - name: failure-models
    version: ^1.0.0

suggests:
  - hot-partitions-and-rebalancing
  - latency-statistics
  - message-ordering-and-partitioning
  - scatter-gather

capabilities:
  - reads-source

agentOverrides:
  codex:
    interface:
      display_name: Distributed Aggregation And Barriers
      short_description: Reduce correctness, barriers and stragglers
      default_prompt: Use Distributed Aggregation And Barriers to check this job's combiner and its synchronisation points.
