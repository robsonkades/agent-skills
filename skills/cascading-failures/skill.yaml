schemaVersion: 1

name: cascading-failures
version: 1.2.0
description: >-
  How one slow dependency becomes a total outage: the amplification loop and the four points
  that close it — retry storms, unbounded queues, thread and connection exhaustion, an inner
  timeout longer than the outer one. Covers why only cutting offered load recovers a
  cascading system, metastable failure where the trigger is gone and the backlog sustains
  it, the thundering herd on recovery, and criticality separation. Use when one dependency's
  latency rise took down services that never call it, when the dependency recovered and the
  system did not, when adding replicas mid-incident made it worse, or when queue depth grows
  while goodput falls to zero. Does not cover the breaker (circuit-breakers), shedding
  policy (rate-limiting-and-load-shedding), bulkheads (concurrency-limiting-and-bulkheads),
  retry policy (retries-and-backoff), queue arithmetic (littles-law-and-queueing), replica
  routing (load-balancing-and-routing), the fault model (failure-models), or the patterns
  individually (distributed-failure-catalogue).

license: Apache-2.0

authors:
  - name: agent-skills contributors
    url: https://github.com/robsonkades/agent-skills

homepage: https://github.com/robsonkades/agent-skills/tree/main/skills/cascading-failures
repository:
  type: git
  url: https://github.com/robsonkades/agent-skills
  directory: skills/cascading-failures

keywords:
  - distributed-systems
  - reliability
  - cascading-failure
  - metastable
  - overload
  - resilience

files:
  - SKILL.md
  - skill.yaml
  - references/

dependencies:
  - name: failure-models
    version: ^1.0.0
  - name: retries-and-backoff
    version: ^1.0.0
  - name: concurrency-limiting-and-bulkheads
    version: ^1.0.0
  - name: rate-limiting-and-load-shedding
    version: ^1.0.0
  - name: timeouts-and-deadlines
    version: ^1.0.0

suggests:
  - caching-strategies
  - circuit-breakers
  - distributed-failure-catalogue
  - distributed-systems-testing
  - kubernetes-service-lifecycle
  - littles-law-and-queueing
  - load-balancing-and-routing
  - load-testing
  - slo-and-alerting
  - task-queues-and-competing-consumers

capabilities:
  - reads-source

agentOverrides:
  codex:
    interface:
      display_name: Cascading Failures
      short_description: How a local failure becomes a total outage
      default_prompt: Use Cascading Failures to find and cut the amplification loop in this system.
