schemaVersion: 1

name: varhandles-and-memory-ordering
version: 1.2.0
description: >-
  Designing and proving low-level Java variable access with VarHandle plain, opaque,
  acquire/release and volatile modes; compare-and-set/exchange, weak CAS, read-modify-write,
  fences, coordinates, signature-polymorphic typing, supported modes, and mixed-access hazards.
  Connects each mode to an algorithmic synchronization edge, allowed outcomes, jcstress model,
  generated-code evidence and target-specific performance measurement. Use only when ordinary
  volatile, atomics, locks or concurrent utilities do not express the required protocol.

license: Apache-2.0

authors:
  - name: agent-skills contributors
    url: https://github.com/robsonkades/agent-skills

homepage: https://github.com/robsonkades/agent-skills/tree/main/skills/varhandles-and-memory-ordering
repository:
  type: git
  url: https://github.com/robsonkades/agent-skills
  directory: skills/varhandles-and-memory-ordering

keywords:
  - java
  - varhandle
  - memory-ordering
  - concurrency
  - acquire-release
  - jcstress
  - aarch64

files:
  - SKILL.md
  - skill.yaml
  - references/

dependencies:
  - name: java-memory-model
    version: ^1.0.0

suggests:
  - lock-free-patterns

capabilities:
  - reads-source

agentOverrides:
  codex:
    interface:
      display_name: VarHandles and Memory Ordering
      short_description: Access modes, barriers and their real cost
      default_prompt: Use VarHandles and Memory Ordering to pick and prove the right access mode here.
